# local-ai-coding-agent
Local Coding Agent — An autonomous developer assistant that converts natural language instructions into actions on a codebase using an LLM (via Ollama). The agent can create files, modify code, execute Python programs, and debug errors inside a secure workspace.
